# Release notes: 2026-05-25 — agent-kit-1.3.3-wp-b2

**Status:** Ready for review  
**Repo:** li-langverse/lidb  
**PR:** (open after push)  
**Author:** agent

---

## Summary (one sentence)

Install shared org agent-kit **1.3.3** and `scripts/sync-agent-kit.sh` so lidb matches org policy while WP-H0 default-branch work proceeds separately.

## Agent continuation (required)

1. Read: `.cursor/agent-kit-version`; `docs/release-notes/2026-05-25-agent-kit-1.3.3-wp-b2.md`.
2. Run: `python3 ../benchmarks/scripts/ensure-org-agent-kit.py --local-only` — expect lidb no longer `missing_kit` after merge.
3. Then: WP-B1 CI workflow after human WP-H0 (`main` default branch).
4. Blocked on: WP-H0 human default-branch rename before `ensure-org-repo-ci` reports green on `main`.

## Changed (specific)

| Area | What | Evidence |
|------|------|----------|
| Agent-kit | `.cursor/**` stamp 1.3.3+12f1f4d190a4dd06 | install-agent-kit.sh |
| Scripts | `scripts/sync-agent-kit.sh`, `expected-agent-kit-version` | repo root |

## Not changed (scope fence)

- Native storage engine / WAL — unchanged.
- `.github/workflows/ci.yml` — unchanged (WP-B1 after WP-H0).
- Default branch name — unchanged until WP-H0.
- `roadmap` agent-kit manifest — WP-A1 PR.

## Breaking changes

None.

## Security

N/A — Cursor policy files only.

## Performance

N/A.

## Downstream

| Repo | Action |
|------|--------|
| li-cursor-agents | N/A — no bulk rebuild |
